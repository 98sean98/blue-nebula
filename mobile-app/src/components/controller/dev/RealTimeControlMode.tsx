import React, { FC } from 'react';
import { Text } from '@ui-kitten/components';

interface RealTimeControlModeProps {}

export const RealTimeControlMode: FC<RealTimeControlModeProps> = () => {
  return <Text>real time control</Text>;
};
