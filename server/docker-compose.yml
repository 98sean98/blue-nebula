version: '3'

services:
  postgres:
    image: 'postgres'
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - scraper_data:/var/lib/postgresql/data/
    ports:
      - 5432:5432

volumes:
  scraper_data:
